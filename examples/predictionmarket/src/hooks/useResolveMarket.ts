import { Address } from "viem"

export const useResolveMarket = () => {

    const resolveMarket = async (resolver: Address) => {
        // By default, all resolvers is the mock resolver
    }

    return { resolveMarket }
}
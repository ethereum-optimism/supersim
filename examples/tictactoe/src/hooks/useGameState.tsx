
/*
const gameAddress: Address = '0x17EEd0e2Eb679927c35333F3Cc258fa2d81609e9'

const clientA = createClient({ chain: supersimChainA, transport: http() })
const clientB = createClient({ chain: supersimChainB, transport: http() })
*/

const useGameState = (gameId: string) => {
    /*
    const [gameState, setGameState] = useState<string>('')

    useEffect(() => {
        const fetchGameState = async () => {
            const state = await getGameState(contractAddress)
            setGameState(state)
        }
    }, [])
    */
}